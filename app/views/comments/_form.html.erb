<%= form_for([@poem, @poem.comments.build]) do |f| %>
  <p>
    <%= f.label :title %><br>
    <%= f.text_field :name %>
  </p>

  <p>
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </p>
  <br>

  <p>
    <%= f.submit %>
  </p>

<% end %>
